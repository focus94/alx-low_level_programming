#include "main.h"

/**
 * mul - check for checks for a didit (0 through 9).
 * @a: a - Variable
 * @b: b - Variable
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int mult;

	mult = a * b;

	return (mult);
}
